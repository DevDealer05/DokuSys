# Fastfile - Deaktiviert (SideStore-Distribution via GitHub Actions)
# Kein Apple Developer Account erforderlich.

default_platform(:ios)

platform :ios do
  desc "Syntax check only (no signing, no TestFlight)"
  lane :test do
    UI.message "Kein Xcode-Projekt vorhanden (Swift Playground Package). Syntax-Check ueber swiftc."
  end

  desc "Nicht verwendet - Build laeuft via .github/workflows/build_ipa.yml"
  lane :beta do
    UI.message "Distribution erfolgt ueber SideStore. Siehe build_ipa.yml."
  end
end
